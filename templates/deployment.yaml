kind: Deployment
apiVersion: apps/v1
metadata:
  name: nginx
  labels:
    app: frontend
    tier: app 
    purpose: Deployment
spec:
  replicas: {{ .Values.deployment.replicas }}
  selector:
    matchLabels: 
      app: frontend
      tier: app
      purpose: Deployment
  template:
    metadata:
      labels: 
        app: frontend
        tier: app
        purpose: Deployment
    spec:
      containers:
      - name: nginx
        image: nginx:{{ .Values.deployment.imageVersion }}
        ports:
        - containerPort: 80





